### This file was generated by Nexus Schema
### Do not make changes to this file directly

type Mutation {
  createDraft(authorEmail: String!, content: String, title: String!): Post!
  createProfile(bio: String, userEmail: String!): Profile!
  deletePost(id: ID): Post!
  deletePosts: Int!
  signupUser(email: String!, name: String): User!
  updateDraft(
    authorEmail: String!
    content: String
    id: ID!
    title: String!
  ): Post!
}

type Post {
  author: User!
  content: String
  id: ID!
  published: Boolean!
  title: String!
}

type Profile {
  bio: String
  id: ID!
  user: User!
}

type Query {
  post(id: ID): Post!
  posts: [Post!]!
  profiles: [Profile!]!
  users: [User!]!
}

type User {
  email: String!
  id: ID!
  name: String
  posts: [Post!]!
  profile: Profile
}
