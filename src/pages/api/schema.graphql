### This file was generated by Nexus Schema
### Do not make changes to this file directly


type CreatePostResponse {
  message: String
  posts: [Post!]
  success: Boolean!
}

type CreateUserResponse {
  message: String
  success: Boolean!
  users: [User!]
}

type Mutation {
  createPost(content: String!, title: String!): CreatePostResponse!
  createUser(email: String!, name: String): CreateUserResponse!
}

type Post {
  author: User
  content: String
  id: ID!
  published: Boolean!
  title: String!
}

type Profile {
  bio: String
  id: ID!
  user: User!
}

type Query {
  post(id: ID): Post!
  posts: [Post!]!
  profiles: [Profile!]!
  users: [User!]!
}

type User {
  email: String!
  id: ID!
  name: String
  posts: [Post!]
  profile: Profile
}
